@model List<WebMediaClient.Models.AlbumViewModel>

<div class="row">
	<div class="col-md-8">
		<div class="list-group">
			@foreach (var item in Model)
			{
				<div class="list-group-item">
					@Html.ActionLink(item.Name, "GetAlbumByID", "Album", new { ID = item.ID }, null);
				</div>
			}
		</div>
	</div>
</div>

