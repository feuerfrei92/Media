@model WebMediaClient.Models.InterestViewModel
<div class="row">
	<div class="col-md-8">
		<div class="list-group">
				<div class="list-group-item">
					@Html.ActionLink(Model.Name, "GetInterestByName", "Interest", new { Name = Model.Name }, null);
				</div>
			}
		</div>
	</div>
</div>
